services:
  portfolio:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cosmic_portfolio
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
